# 设计规范

## 页面布局

### 顶部工具栏与内容区间距

- 顶部不再使用页面级标题区（如 `PageHeader`）造成的大块留白时，推荐使用“右上角工具栏”承载主要操作按钮。
- **工具栏与下方内容区的垂直间距统一使用工具栏容器的 `mb-3`**。
- **内容区本身不再额外添加 `mt-*`**（避免不同页面/不同状态分支出现间距不一致）。

推荐结构：

- 顶部工具栏：`className="flex items-center justify-end mb-3"`
- 内容容器：`className="flex-1 min-h-0 flex flex-col"`

