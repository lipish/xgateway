# XGateway API Access Control & Routing E2E Test Report (2026-03-07)

## 1. Overview
This report documents the verification of API Key binding to multiple model instances, load balancing strategies, and automatic fallback mechanisms in XGateway.

**Test Environment:**
- **Version:** XGateway v0.13.5
- **Database:** `xgateway_new` (Production-like schema with real provider keys)
- **Port:** 3010

---

## 2. Test Scenarios & Results

### Scenario A: Multi-Instance Binding (Access Control)
- **Goal:** Verify that an API Key restricted to specific `provider_ids` can only access those instances.
- **Setup:** API Key bindings `[5, 11]`. Attempt access to `provider_id=6`.
- **Result:** **SUCCESS**
  - Requests to Providers 5 & 11 succeeded with 200 OK.
  - Request to Provider 6 returned **403 Forbidden**.
  - `GET /v1/models` only returned models from allowed providers.

### Scenario B: Round-Robin Load Balancing
- **Goal:** Verify cyclical distribution of requests across multiple healthy providers.
- **Setup:** Strategy `RoundRobin` with providers `[5, 11]`.
- **Result:** **SUCCESS**
  - Sequence of 3 requests: **Provider 5 -> Provider 11 -> Provider 5**.
  - Verified via `request_logs`.

### Scenario C: Automatic Fallback (Retry Logic)
- **Goal:** Verify that the gateway transparently handles a failed primary provider by retrying a healthy one.
- **Setup:** 
  - Provider 777 (Invalid Key, High Priority)
  - Provider 5 (Valid Key, Low Priority)
- **Result:** **SUCCESS**
  - Internal log recorded `401 Unauthorized` for Provider 777.
  - Gateway automatically retried Provider 5.
  - User received a successful response (200 OK) without seeing the internal retry.

### Scenario D: Error Propagation
- **Goal:** Verify that upstream errors are correctly mapped to OpenAI format when no fallbacks remain.
- **Setup:** Key restricted to a single invalid provider.
- **Result:** **SUCCESS**
  - Unified error message returned: `API error: Authentication failed: OpenAI: HTTP 401 Unauthorized...`

---

## 3. Verified Performance Metrics (Real Inference)
| Provider | Model | Status | Latency | Result Content |
| :--- | :--- | :--- | :--- | :--- |
| OpenAI Proxy | gpt-5.2 | Success | ~1.6s | Verified Real Output |
| MiniMax | gpt-4o | Success | ~2.1s | Verified Real Output |
| Invalid-Stub | gpt-fake | Error | ~0.1s | 401/403 Correctly Logged |

---
*Generated by GitHub Copilot on 2026-03-07*
