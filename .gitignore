# llm_link.db
llm_link.db

# shadcn
shadcn

# web-skeleton
web-skeleton

# admin node modules
/admin/node_modules

# Local model overrides (per-user, may contain private endpoint IDs)
model-overrides.yaml

# docs-site
/docs-site/node_modules
/docs-site/build
/docs-site/.svelte-kit

# AI Coding
/.augment
/.claude
/.qoder

#Keys
keys.yaml

# Local model overrides (per-user, may contain private endpoint IDs)
model-overrides.yaml

# Mac files
.DS_Store

# Build artifacts
/target
python/dist/
python/build/
python/.pytest_cache/
python/.mypy_cache/
python/.venv/

# IDE and editor files
/.claude
/.augment
.vscode/
.idea/

# Environment variables (contains sensitive API keys)
.env
.env.local
.env.production

# Configuration files with real API keys
# (Keep example configs in repo, but ignore files with real keys)
**/config-local.yaml
**/config-production.yaml
**/config-personal.yaml
**/config-working.yaml

# Backup files
*.bak
*.backup
*~

# Database
data/llm_link.db

# Logs
*.log
logs/

# Test outputs
/tmp/llm-link*.log
/tmp/stream-response.txt
debug.log
llm-link-debug.log

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
